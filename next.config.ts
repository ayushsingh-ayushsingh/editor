import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["lh3.googleusercontent.com", "external-content.duckduckgo.com"]
  }
};

export default nextConfig;
